@page
@model FacturaMRC.Pages.DetailBills.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DetailBill[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetailBill[0].Bill)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetailBill[0].Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DetailBill) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bill.CodeBill)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.Code)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DetailBillID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DetailBillID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DetailBillID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
